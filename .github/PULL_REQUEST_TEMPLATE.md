<!--

The code checklist:

- The unit test suite is green (just test)
- The coverage on edited files is 100% (just diff-cover)
- The documentation can build (just doc)
- Complex administrator procedures, and technical information are documented

The PR message checklist:

- The message describes what changes from the end-user point of view (what is visible from the interface, changes in behaviors etc.)
- The message describes what changes from the administrator point of view (new migrations, configuration parameter changes, details on performances points of attention etc.)
- The message includes screenshots for UI changes and novelties
-->
