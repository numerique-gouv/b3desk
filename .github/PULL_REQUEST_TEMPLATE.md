<!--

The code checklist:

- The unit test suite is green (just test)
- The coverage on edited files is 100% (just diff-cover)
- The documentation can build (just doc)
- Complex administrator procedures, and technical information are documented

The changelog checklist:

- A changelog fragment has been added (just changelog), or the "skip changelog"
  label is set because nothing changes for the users nor the administrators
- The fragment describes what changes from the end-user point of view (what is
  visible from the interface, changes in behaviors etc.)
- The fragment describes what changes from the administrator point of view (new
  migrations, configuration parameter changes, details on performances points
  of attention etc.)

The PR message checklist:

- The message includes screenshots for UI changes and novelties
-->
